apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'first'

dependencies {
    compile files('libs/algs4.jar')
}

// tag::repositories[]
repositories {
    mavenCentral()
}
// end::repositories[]
